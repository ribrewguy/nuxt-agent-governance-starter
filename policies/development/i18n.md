# i18n Policy

## Localization Sync

* `i18n/locales/en.json` is the source of truth.
* Every key in `i18n/locales/en.json` MUST exist in every other localization file.
* When adding, removing, or changing keys in `en.json`, update all other locale files in the same change.
