import { describe, it, expect, beforeEach } from 'vitest'

describe('DummyTest', () => {
  beforeEach(() => {
  })

  describe('emptyTest', () => {
    it('should pass', () => {
      expect(true).toBe(true)
    })
  })
})
